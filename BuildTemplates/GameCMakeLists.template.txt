# CMakeLists.txt for the game directory

# create the games target
#add_executable(Game
#	src/main.cpp
#)

kobalt_add_lib(Game)


find_package(SDL3 REQUIRED)
find_package(GLEW REQUIRED)
if (WIN32)
    add_custom_command(
        TARGET Game POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_if_different
        "$<TARGET_FILE:SDL3::SDL3>" "$<TARGET_FILE_DIR:Game>"
        COMMAND ${CMAKE_COMMAND} -E copy_if_different
        "$<TARGET_FILE:GLEW::GLEW>" "$<TARGET_FILE_DIR:Game>"
        VERBATIM
    )
endif()

set_target_properties(Game PROPERTIES FOLDER Game)